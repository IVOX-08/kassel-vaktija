<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Brand colors (mirrored in Compose theme/Color.kt). -->
    <color name="brand_green">#2E7D32</color>
    <color name="brand_gold">#B8860B</color>

    <!-- Window background used by the launch theme before Compose draws. -->
    <color name="window_background">#FFFFFFFF</color>

    <!-- Adaptive launcher icon background: transparent, so only the shield shows
         (no white tile/frame behind it). -->
    <color name="ic_launcher_background">#00000000</color>
</resources>
